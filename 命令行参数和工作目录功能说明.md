# 命令行参数和工作目录功能说明

## 功能概述

已为 TrayLauncher 添加了以下功能：
1. **命令行参数支持**：可以为每个菜单项设置命令行参数
2. **工作目录支持**：可以为每个菜单项设置程序的工作目录

## 配置文件格式

### 文本格式 (TLCmd.txt)

原有的格式：
```
菜单名称 = 路径|||图标路径
```

新格式（支持工作目录）：
```
菜单名称 = 路径|||图标路径|||工作目录
```

**示例：**
```
记事本 = C:\Windows\notepad.exe|||C:\Windows\System32\notepad.exe|||C:\Users\MyUser\Documents
```

如果不需要图标或工作目录，可以省略：
```
记事本 = C:\Windows\notepad.exe
记事本 = C:\Windows\notepad.exe|||C:\Windows\System32\notepad.exe
记事本 = C:\Windows\notepad.exe|||C:\Windows\System32\notepad.exe|||C:\Users\MyUser\Documents
```

### XML 格式

新增了 `workdir` 属性：

```xml
<item name="记事本" path="C:\Windows\notepad.exe" icon="C:\Windows\System32\notepad.exe" workdir="C:\Users\MyUser\Documents"/>
```

## 命令行参数

命令行参数可以通过在路径中使用引号和空格来指定：

```
程序名 = "C:\Program Files\App\app.exe" 参数1 参数2
```

或者使用配置文件格式：
```
程序名 = C:\Program Files\App\app.exe|||图标路径|||工作目录
```

**注意**：命令行参数会自动从路径中解析出来（如果路径包含引号和空格）。

## 工作目录

工作目录支持：
- 相对路径（相对于程序所在目录）
- 绝对路径
- 环境变量（如 `%USERPROFILE%\Documents`）

如果未指定工作目录，程序将使用可执行文件所在的目录作为工作目录（原有行为）。

## 代码更改

### 1. CItem 类扩展

- 添加了 `WorkDir()` 方法
- 构造函数现在接受工作目录参数
- `m_str` 向量从 3 个元素扩展到 4 个元素

### 2. MenuWithIcon 类扩展

- 添加了 `m_ItemWorkDir` 映射来存储工作目录
- 添加了 `WorkDir(nID)` 方法来获取工作目录
- `AddMenuItem` 函数现在接受工作目录参数

### 3. ExecuteEx 函数增强

- 添加了 `strWorkDir` 参数
- 支持自定义工作目录
- 自动展开环境变量
- 自动转换为绝对路径

### 4. 配置文件解析

- `SepPathIconAndWorkDir` 函数解析路径、图标和工作目录
- 支持文本格式和 XML 格式

## 使用示例

### 示例 1：启动程序并设置工作目录

```
我的项目 = "C:\Program Files\MyApp\app.exe"|||C:\Program Files\MyApp\app.exe|||D:\MyProject
```

### 示例 2：带命令行参数和工作目录

```
编译项目 = "C:\Program Files\MSBuild\MSBuild.exe" /p:Configuration=Release|||C:\Program Files\MSBuild\MSBuild.exe|||D:\MyProject
```

### 示例 3：使用环境变量

```
打开文档 = C:\Windows\notepad.exe|||C:\Windows\System32\notepad.exe|||%USERPROFILE%\Documents
```

## 向后兼容性

- 现有的配置文件（不包含工作目录）仍然可以正常工作
- 如果未指定工作目录，程序行为与之前相同（使用程序所在目录）
- 命令行参数功能已经存在，现在与工作目录功能配合使用

## 注意事项

1. 工作目录路径中的环境变量会自动展开
2. 相对路径会转换为绝对路径
3. 如果指定的工作目录不存在，程序可能无法正常启动
4. 工作目录仅对可执行文件有效，对文件夹或文档无效

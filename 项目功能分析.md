# TrayLauncher 项目功能分析

## 项目概述

**TrayLauncher** 是一个 Windows 系统托盘启动器应用程序，允许用户通过系统托盘图标快速访问常用程序和文件。这是一个用 C++ 开发的 Windows 原生应用程序，支持自定义菜单、热键、多语言和皮肤主题。

---

## 核心功能模块

### 1. **系统托盘集成** (`TrayStart.cpp`, `MsgMap.cpp`)

- **托盘图标管理**
  - 在系统托盘显示图标
  - 支持自定义托盘图标
  - 托盘图标提示信息（Tooltip）
  - 托盘通知消息（Balloon Tips）

- **单实例运行**
  - 使用 Mutex 确保只有一个实例运行
  - 如果已有实例，会激活现有窗口而不是启动新实例
  - 支持通过 `--single-instance=false` 参数禁用单实例模式

### 2. **菜单系统** (`MenuWithIcon.h/cpp`, `MenuData.h/cpp`)

- **动态菜单构建**
  - 从配置文件（`TLCmd.txt` 或 XML）加载菜单结构
  - 支持多级嵌套菜单
  - 支持静态菜单和动态菜单
  - 动态菜单可以实时扫描目录内容

- **菜单项类型**
  - **文件/程序项**：直接启动文件或程序
  - **目录项**：展开显示目录内容
  - **通配符项**：使用通配符匹配多个文件（如 `*.exe`）
  - **展开项**：动态展开目录内容

- **图标支持**
  - 自动提取文件和文件夹图标
  - 支持自定义图标路径
  - 支持快捷方式（.lnk）图标
  - 支持不同尺寸图标（16x16, 32x32）

- **菜单显示选项**
  - 显示/隐藏隐藏文件
  - 过滤空子菜单
  - 支持"在此处打开"功能

### 3. **配置文件管理** (`SettingFile.h/cpp`)

- **INI 格式配置**
  - 使用 `TL.ini` 存储设置
  - 支持多个配置节（Section）
  - 主要配置项：
    - `general`: 通用设置（语言、皮肤、优先级等）
    - `hotkey`: 热键配置
    - `history`: 运行对话框历史记录
    - `color`: 颜色设置

- **配置项说明**
  - `language`: 语言文件路径
  - `skin`: 皮肤主题
  - `showhidden`: 是否显示隐藏文件
  - `midclick`: 是否启用中键点击
  - `priority`: 进程优先级（idle, normal, high 等）
  - `confirm_exit`: 退出时确认

### 4. **热键系统** (`Hotkey.h/cpp`)

- **全局热键支持**
  - 注册系统级全局热键
  - 支持组合键（Ctrl, Alt, Shift, Win）
  - 默认热键：
    - `Alt + LWin`: 显示主菜单
    - `Alt + RWin`: 显示系统菜单
    - `Shift + LWin`: 在鼠标位置显示菜单
    - `Ctrl + LWin`: 显示运行对话框
    - `Ctrl + Alt + LWin`: 显示系统菜单（备用）

- **热键管理**
  - 动态添加/删除热键
  - 热键冲突检测
  - 热键配置持久化

### 5. **运行对话框** (`RunDlg.h/cpp`)

- **快速启动对话框**
  - 类似 Windows 运行对话框
  - 支持命令历史记录
  - 自动补全功能
  - 支持路径和程序名输入
  - 记住对话框位置

- **命令执行**
  - 支持直接执行程序
  - 支持命令行参数
  - 支持 Shell 命令

### 6. **鼠标钩子** (`CB/dll/main.cpp` - TL.dll)

- **全局鼠标钩子**
  - 通过 DLL 实现全局鼠标钩子
  - 捕获中键点击事件
  - 支持中键点击显示菜单功能
  - 防止重复触发（1秒内只响应一次）

### 7. **多语言支持** (`language.h/cpp`)

- **国际化**
  - 支持多种语言文件（`.lng`）
  - 语言文件存储在 `lng/` 目录
  - 支持的语言包括：中文、英文、德语、法语、意大利语、韩语、荷兰语、波兰语、罗马尼亚语、俄语、瑞典语等
  - 动态切换语言
  - 语言菜单显示语言名称和版本信息

### 8. **皮肤系统**

- **主题支持**
  - 多个内置皮肤主题（存储在 `skin/` 目录）
  - 支持自定义图标集
  - 皮肤包括：
    - default（默认）
    - blue, bluelight
    - green, greenlight, green_2
    - gray, graylight
    - red, redlight
    - darkbargreen, darkbarneongreen
    - Office, Reactos
    - win95, win311
    - windows classic
    - 等等

- **图标自定义**
  - 每个皮肤可以自定义菜单图标
  - 支持文件夹打开/关闭图标
  - 支持未知文件类型图标

### 9. **菜单配置工具** (`TLMenuCfg/`)

- **图形化配置界面**
  - 使用 wxWidgets 构建的配置工具
  - 树形结构编辑菜单
  - 可视化添加/删除/编辑菜单项
  - 支持拖放操作
  - 保存为文本或 XML 格式

### 10. **文件操作** (`FileStrFnc.h/cpp`)

- **文件和目录操作**
  - 文件搜索和过滤
  - 目录遍历
  - 通配符匹配
  - 文件属性检查
  - Shell 操作（打开、运行等）

### 11. **XML 支持** (`xmlmenudata.h/cpp`)

- **XML 格式菜单**
  - 使用 pugixml 库解析 XML
  - 支持 XML 格式的菜单配置文件
  - XML 和文本格式互相转换

### 12. **其他功能**

- **进程优先级控制**
  - 可设置进程优先级（idle, below_normal, normal, above_normal, high）

- **自动启动**
  - 支持添加到 Windows 启动项
  - 开机自动运行

- **文件监控**
  - 自动检测配置文件变化
  - 菜单自动刷新

- **命令历史**
  - 运行对话框历史记录
  - 快速访问常用命令

---

## 项目结构

```
traylauncher/
├── src/                    # 主程序源代码
│   ├── include/            # 头文件
│   └── source/             # 源文件
│       ├── TrayStart.cpp   # 程序入口
│       ├── MsgMap.cpp      # 消息处理和菜单显示
│       ├── MenuData.cpp    # 菜单数据结构
│       ├── MenuWithIcon.cpp # 带图标的菜单
│       ├── RunDlg.cpp      # 运行对话框
│       ├── Hotkey.cpp      # 热键处理
│       ├── SettingFile.cpp # 配置文件管理
│       └── ...
├── TLMenuCfg/              # 菜单配置工具
│   ├── TLMenuCfgApp.cpp   # 配置工具入口
│   └── TLMenuCfgMain.cpp  # 配置工具主窗口
├── CB/dll/                 # 鼠标钩子 DLL
│   └── main.cpp            # 钩子实现
├── lng/                    # 语言文件
├── skin/                   # 皮肤主题
└── latest/                 # 发布文件
```

---

## 配置文件格式

### 菜单配置文件 (`TLCmd.txt`)

```
菜单名称 = 路径或命令
   子菜单项1 = 路径1
   子菜单项2 = 路径2
   |||自定义图标路径
   子菜单项3 = 路径3
```

### 设置文件 (`TL.ini`)

```ini
[general]
language=
skin=
showhidden=0
midclick=0
priority=normal

[hotkey]
ShowMenu=Alt + LWin
ShowMenuAtMouse=Shift + LWin
ShowContextMenu=Alt + RWin
RunDialog=Ctrl + LWin
```

---

## 技术特点

1. **Windows API 原生开发**
   - 使用 Win32 API
   - 系统托盘集成（Shell_NotifyIcon）
   - 全局钩子（SetWindowsHookEx）
   - COM 接口（图标提取）

2. **面向对象设计**
   - 清晰的类层次结构
   - 资源管理（RAII）
   - 智能指针使用

3. **可扩展性**
   - 插件式架构（DLL 钩子）
   - 配置文件驱动
   - 多语言和皮肤支持

4. **用户体验**
   - 快速响应
   - 丰富的自定义选项
   - 直观的界面

---

## 使用场景

1. **快速启动常用程序**
   - 通过托盘图标快速访问常用应用
   - 减少桌面图标和开始菜单使用

2. **文件管理**
   - 快速访问常用目录
   - 文件浏览器功能

3. **工作流优化**
   - 自定义工作流程菜单
   - 快速执行常用命令

4. **系统集成**
   - 集成到 Windows 工作流
   - 替代或补充开始菜单

---

## 总结

TrayLauncher 是一个功能完善的 Windows 托盘启动器，提供了丰富的自定义选项和良好的用户体验。它通过配置文件驱动的方式，允许用户完全自定义菜单结构，同时支持多语言、皮肤主题、热键等高级功能，是一个实用的 Windows 生产力工具。
